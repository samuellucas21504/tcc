version: "2"
services:
  mysql:
    container_name: mysql
    restart: always
    image: mysql:8
    ports:
      - 3306:3306
    expose:
      - 3306
    environment:
      MYSQL_ROOT_PASSWORD: "root" # TODO: Change this

    volumes:
      - my-datavolume:/var/lib/mysql
volumes:
  my-datavolume:
